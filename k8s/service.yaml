apiVersion: v1
kind: Service
metadata:
  name: northwind-logistics-service
spec:
  type: NodePort
  selector:
    app: northwind-logistics
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080